services:
  app:
    build: .
    container_name: workdesk24-api
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: root@mysql
      DB_NAME: workdesk24
    command: npm start