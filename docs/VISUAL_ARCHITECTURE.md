# Visual Architecture Guide

## 📊 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT APPLICATIONS                      │
│  (Web Browser, Mobile App, Postman, cURL, External Services)    │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               │ HTTP/HTTPS Requests
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                        EXPRESS.JS SERVER                         │
│                      (Port 3000 / app.js)                        │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                ┌──────────────┴──────────────┐
                │                             │
        ┌───────▼────────┐           ┌───────▼────────┐
        │  MIDDLEWARE    │           │  ERROR HANDLER │
        │  LAYER         │           │                │
        └───────┬────────┘           └────────────────┘
                │
    ┌───────────┼───────────┐
    │           │           │
┌───▼────┐ ┌───▼────┐ ┌───▼────┐
│ CORS   │ │ JSON   │ │ RATE   │
│        │ │ PARSER │ │ LIMITER│
└────────┘ └────────┘ └───┬────┘
                           │
                    ┌──────▼──────┐
                    │ JWT AUTH    │
                    │ (Protected) │
                    └──────┬──────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   API ROUTER (/api)                      │
└──────────────────────────┬──────────────────────────────┘
                           │
            ┌──────────────┼──────────────┐
            │              │              │
    ┌───────▼───────┐ ┌───▼────┐ ┌──────▼──────┐
    │ Legacy Routes │ │ Health │ │ V1 API      │
    │ /auth         │ │ Check  │ │ /v1/*       │
    │ /users        │ │        │ │             │
    └───────────────┘ └────────┘ └──────┬──────┘
                                         │
                        ┌────────────────┼────────────────┐
                        │                │                │
                ┌───────▼───────┐ ┌─────▼─────┐ ┌───────▼───────┐
                │  PUBLIC       │ │   APP     │ │    ADMIN      │
                │  /v1/public/* │ │ /v1/app/* │ │ /v1/admin/*   │
                │ (No Auth)     │ │ (Auth)    │ │ (Auth+Role)   │
                └───────┬───────┘ └─────┬─────┘ └───────┬───────┘
                        │               │               │
                ┌───────▼───────────────▼───────────────▼───────┐
                │            CONTROLLERS LAYER                   │
                │  (Request Validation & Business Logic Call)    │
                └───────┬───────────────┬───────────────┬────────┘
                        │               │               │
                ┌───────▼───────┐ ┌────▼─────┐ ┌──────▼──────┐
                │ PUBLIC        │ │   APP    │ │   ADMIN     │
                │ Controllers   │ │Controllers│ │ Controllers │
                └───────┬───────┘ └────┬─────┘ └──────┬──────┘
                        │               │              │
                        └───────────────┼──────────────┘
                                        │
                        ┌───────────────▼───────────────┐
                        │       SERVICES LAYER          │
                        │  (Business Logic & External)  │
                        └───────┬───────────┬───────────┘
                                │           │
                    ┌───────────┼───────────┼───────────┐
                    │           │           │           │
            ┌───────▼──────┐ ┌─▼────┐ ┌───▼──────┐ ┌──▼─────┐
            │ Auth Service │ │Email │ │  Upload  │ │ Future │
            │ (JWT Ops)    │ │Service│ │ Service  │ │Services│
            └───────┬──────┘ └─┬────┘ └───┬──────┘ └────────┘
                    │          │           │
                    │     ┌────▼──────┐    │
                    │     │ Nodemailer│    │
                    │     │ (SMTP)    │    │
                    │     └───────────┘    │
                    │                      │
            ┌───────▼──────────────────────▼───────┐
            │         MODELS LAYER (Sequelize)     │
            │  (Data Access & Database Operations) │
            └───────┬──────────────────────┬───────┘
                    │                      │
            ┌───────▼───────┐      ┌──────▼──────┐
            │  User Model   │      │Inquiry Model│
            │  (users)      │      │ (inquiries) │
            └───────┬───────┘      └──────┬──────┘
                    │                     │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   MySQL Database    │
                    │   (workdesk24)      │
                    └─────────────────────┘
```

---

## 🔄 Request Flow Examples

### Example 1: Public Inquiry Submission

```
1. Client POSTs to /api/v1/public/inquiries
   │
2. Express receives request
   │
3. ├─→ CORS middleware (allow)
   ├─→ JSON parser (parse body)
   └─→ Rate limiter (5/hour check)
   │
4. Routes to Public Inquiry Controller
   │
5. Controller validates input
   │
6. ├─→ Inquiry created in database via Model
   │
7. ├─→ Email Service sends admin notification
   └─→ Email Service sends user confirmation
   │
8. Returns 201 Created with inquiry ID
```

### Example 2: Admin Get Inquiries (Authenticated)

```
1. Client GETs /api/v1/admin/inquiries with Bearer token
   │
2. Express receives request
   │
3. ├─→ CORS middleware (allow)
   ├─→ JSON parser
   ├─→ Rate limiter (500/15min check)
   └─→ JWT Auth middleware
       │
       ├─→ Verify token signature
       ├─→ Check token expiration
       ├─→ Load user from database
       └─→ Attach req.user
   │
4. Routes to Admin Inquiry Controller
   │
5. Controller parses pagination params
   │
6. Service queries database with filters
   │
7. Returns paginated inquiry list with metadata
```

### Example 3: App Profile Update (Authenticated)

```
1. Client PUTs /api/v1/app/profile with Bearer token
   │
2. Express middleware chain
   │
3. ├─→ CORS, JSON parser, Rate limiter (300/15min)
   └─→ JWT Auth middleware (verify + load user)
   │
4. Routes to App Profile Controller
   │
5. Controller validates update data
   │
6. Service updates user record
   │
7. Returns updated profile (password excluded)
```

---

## 📁 Folder Structure Mapping

```
Physical Files                      Logical Layer
─────────────────                   ─────────────
src/app.js                    →     Express Application Setup
src/server.js                 →     HTTP Server & Port Binding

src/middleware/               →     Middleware Layer
├── auth.js                   →       JWT Verification
├── rateLimiter.js           →       Rate Limiting
├── errorHandler.js          →       Global Error Handler
├── validate.js              →       Input Validation
└── upload.js                →       File Upload Handler

src/routes/                   →     Routing Layer
├── index.js                 →       Main Router
└── v1/                      →       Version 1 Routes
    ├── public/              →         Public Endpoints
    ├── app/                 →         App Endpoints
    └── admin/               →         Admin Endpoints

src/controllers/v1/          →     Controller Layer
├── public/                  →       Request Handlers (Public)
├── app/                     →       Request Handlers (App)
└── admin/                   →       Request Handlers (Admin)

src/services/                →     Service Layer
├── authService.js          →       JWT Operations
├── emailService.js         →       Email Operations
└── [future services]       →       Additional Business Logic

src/models/                  →     Data Access Layer
├── index.js                →       Model Loader
├── user.js                 →       User Model & Associations
└── inquiry.js              →       Inquiry Model & Associations

src/migrations/             →     Database Schema Versions
src/config/                 →     Configuration Files
src/utils/                  →     Helper Functions
```

---

## 🛣️ API Route Tree

```
/api
├── /health                          [GET]    Legacy health check
├── /auth
│   ├── /register                    [POST]   User registration
│   └── /login                       [POST]   User login
├── /users                           [GET]    Legacy user list
│   └── /:id                         [GET]    Legacy user details
│
└── /v1                              ──────── VERSION 1 API
    ├── /public                      ──────── No authentication
    │   ├── /info
    │   │   ├── /health             [GET]    Health check
    │   │   └── /version            [GET]    API version
    │   └── /inquiries              [POST]   Submit inquiry (5/hour)
    │
    ├── /app                         ──────── User authentication
    │   ├── /profile                [GET]    Get profile
    │   │                           [PUT]    Update profile
    │   │   └── /password           [PUT]    Change password
    │   └── /data                   [GET]    Get data
    │                               [POST]   Create data
    │
    └── /admin                       ──────── Admin authentication
        ├── /users                  [GET]    List users
        │   └── /:id                [GET]    Get user
        │                           [PUT]    Update user
        │                           [DELETE] Delete user
        │
        ├── /inquiries              [GET]    List inquiries
        │   └── /:id                [GET]    Get inquiry
        │       │                   [PUT]    Update inquiry
        │       │                   [DELETE] Delete inquiry
        │       ├── /assign         [POST]   Assign to admin
        │       └── /status         [PUT]    Update status
        │
        └── /dashboard
            ├── /stats              [GET]    Statistics
            └── /recent-inquiries   [GET]    Recent list
```

---

## 🔐 Authentication Flow

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │ 1. POST /api/auth/login
       │    { email, password }
       ▼
┌─────────────────┐
│ Auth Controller │
└──────┬──────────┘
       │ 2. Validate credentials
       ▼
┌─────────────────┐
│  Auth Service   │
└──────┬──────────┘
       │ 3. Query User model
       ▼
┌─────────────────┐
│   User Model    │
└──────┬──────────┘
       │ 4. Find user by email
       ▼
┌─────────────────┐
│  MySQL Database │
└──────┬──────────┘
       │ 5. Return user record
       ▼
┌─────────────────┐
│  Auth Service   │
└──────┬──────────┘
       │ 6. Compare password (bcrypt)
       │ 7. Generate JWT token
       ▼
┌─────────────────┐
│ Auth Controller │
└──────┬──────────┘
       │ 8. Return { token, user }
       ▼
┌─────────────┐
│   Client    │ (Stores token)
└─────────────┘

─────────────────────────────────────

┌─────────────┐
│   Client    │
└──────┬──────┘
       │ 1. GET /api/v1/app/profile
       │    Authorization: Bearer <token>
       ▼
┌─────────────────┐
│  Auth Middleware│
└──────┬──────────┘
       │ 2. Extract token from header
       │ 3. Verify JWT signature
       │ 4. Check expiration
       ▼
┌─────────────────┐
│   User Model    │
└──────┬──────────┘
       │ 5. Load user by ID from token
       ▼
┌─────────────────┐
│  Auth Middleware│
└──────┬──────────┘
       │ 6. Attach req.user
       ▼
┌─────────────────┐
│Profile Controller│
└──────┬──────────┘
       │ 7. Access req.user.id
       │ 8. Return profile data
       ▼
┌─────────────┐
│   Client    │
└─────────────┘
```

---

## 📧 Email Notification Flow

```
┌────────────┐
│   Client   │
└─────┬──────┘
      │ POST /api/v1/public/inquiries
      ▼
┌──────────────────┐
│Inquiry Controller│
└─────┬────────────┘
      │ 1. Validate input
      ▼
┌──────────────────┐
│  Inquiry Model   │
└─────┬────────────┘
      │ 2. Save to database
      ▼
┌──────────────────┐
│ Email Service    │
└─────┬─────┬──────┘
      │     │
      │     └──────────────────┐
      │                        │
      ▼                        ▼
┌──────────────────┐  ┌──────────────────┐
│ Admin            │  │ User             │
│ Notification     │  │ Confirmation     │
└─────┬────────────┘  └─────┬────────────┘
      │                     │
      ▼                     ▼
┌──────────────────┐  ┌──────────────────┐
│  Nodemailer      │  │  Nodemailer      │
│  (SMTP)          │  │  (SMTP)          │
└─────┬────────────┘  └─────┬────────────┘
      │                     │
      ▼                     ▼
┌──────────────────┐  ┌──────────────────┐
│ admin@domain.com │  │user@example.com  │
└──────────────────┘  └──────────────────┘
```

---

## 🚦 Rate Limiting Strategy

```
Route Type    Window      Max Requests    Use Case
──────────    ──────      ────────────    ────────
Public        15 minutes  100             General API access
Inquiry       1 hour      5               Prevent spam submissions
Auth          15 minutes  10              Brute force protection
Admin         15 minutes  500             High-capacity admin ops
App           15 minutes  300             Moderate user capacity

Implementation:
┌─────────────┐
│   Client    │
└──────┬──────┘
       │ Request
       ▼
┌─────────────────┐
│ Rate Limiter    │
│ Middleware      │
└──────┬──────────┘
       │ Check IP + Route
       ▼
┌─────────────────┐
│ In-Memory Store │  (Redis in production)
│ { IP: count }   │
└──────┬──────────┘
       │
       ├─→ Under limit?  → Continue
       │
       └─→ Over limit?   → 429 Too Many Requests
```

---

## 🎯 Microservice Extraction Path

### Current Monolith
```
┌────────────────────────────────────────┐
│         Express Application            │
│  ┌──────────┐  ┌──────────┐           │
│  │ Inquiry  │  │   User   │           │
│  │ Module   │  │  Module  │           │
│  └────┬─────┘  └────┬─────┘           │
│       │             │                  │
│  ┌────▼─────────────▼─────┐           │
│  │    MySQL Database       │           │
│  └─────────────────────────┘           │
└────────────────────────────────────────┘
```

### Future Microservices
```
┌──────────────────────────────────────────────┐
│           API Gateway / Load Balancer         │
└─────┬────────────┬────────────┬───────────────┘
      │            │            │
      ▼            ▼            ▼
┌───────────┐ ┌───────────┐ ┌────────────────┐
│ Inquiry   │ │   User    │ │  Notification  │
│ Service   │ │  Service  │ │    Service     │
│           │ │           │ │                │
│ Port 3001 │ │ Port 3002 │ │   Port 3003    │
└─────┬─────┘ └─────┬─────┘ └────────┬───────┘
      │             │                 │
      ▼             ▼                 ▼
┌───────────┐ ┌───────────┐    ┌──────────┐
│ Inquiry   │ │   User    │    │  Queue   │
│    DB     │ │    DB     │    │ (Redis)  │
└───────────┘ └───────────┘    └──────────┘
```

**Benefits of Current Design:**
- Controllers already domain-separated (public, app, admin)
- Services layer isolates business logic
- Stateless JWT authentication (no shared session)
- Database models can be duplicated per service
- API versioning allows gradual migration

---

## 📊 Data Flow: Inquiry Submission

```
Time →

Client          Router          Controller       Service         Model          Database       Email
  │               │                │              │               │               │              │
  ├─POST inquiry─→│                │              │               │               │              │
  │               ├─rate limit────→│              │               │               │              │
  │               │  check OK      │              │               │               │              │
  │               ├─route──────────→│              │               │               │              │
  │               │                ├─validate─────→│               │               │              │
  │               │                │              ├─create────────→│               │              │
  │               │                │              │               ├─INSERT────────→│              │
  │               │                │              │               │←─inquiry_id───┤              │
  │               │                │              │←─inquiry obj──┤               │              │
  │               │                │              ├─notify admin──┼───────────────┼──────────────→│
  │               │                │              │               │               │    ┌──────┐  │
  │               │                │              │               │               │    │SMTP  │  │
  │               │                │              │               │               │    │Send  │  │
  │               │                │              │               │               │    └───┬──┘  │
  │               │                │              │               │               │        │OK   │
  │               │                │              │←──────────────┼───────────────┼────────┘     │
  │               │                │              ├─confirm user──┼───────────────┼──────────────→│
  │               │                │              │               │               │    ┌──────┐  │
  │               │                │              │               │               │    │SMTP  │  │
  │               │                │              │               │               │    │Send  │  │
  │               │                │              │               │               │    └───┬──┘  │
  │               │                │              │               │               │        │OK   │
  │               │                │              │←──────────────┼───────────────┼────────┘     │
  │               │                │←─success─────┤               │               │              │
  │               │←─201 Created──┤              │               │               │              │
  │←─response─────┤                │              │               │               │              │
  │  {id, msg}    │                │              │               │               │              │
```

---

## 🔄 Continuous Flow Patterns

### Development Workflow
```
Code Change → Nodemon Restart → Database Migration (if needed) → Test Endpoint → Verify Result
```

### Deployment Workflow
```
Git Push → CI/CD Pipeline → Run Tests → Build → Deploy to Staging → Run E2E Tests → Deploy to Production
```

### Request-Response Cycle
```
HTTP Request → Middleware Stack → Route Matching → Controller Logic → Service Call → Database Query → Response Formatting → HTTP Response
```

---

**Use this visual guide alongside:**
- `ARCHITECTURE.md` for detailed explanations
- `API_TESTING.md` for practical examples
- `PROJECT_SUMMARY.md` for quick reference
